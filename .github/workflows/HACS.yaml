name: HACS Action

on: 
  push: 


jobs:
  hacs:
    name: HACS Action
    runs-on: ubuntu-latest
      steps: 
      - name: HACS Action
        uses: hacs/action@main
        with:
          category: "integration"